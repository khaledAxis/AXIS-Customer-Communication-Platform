# Background work

Pure job capability limits, bounded retry timing and typed delivery confirmation. Jobs do not
grant authority: the server validates the stored delegating user and the current lease on execution.
