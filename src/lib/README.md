# `src/lib` — Shared, framework-agnostic utilities

Small, pure helpers reused across layers: validation helpers, formatting, date/normalization
utilities. No framework, database, or vendor imports. If a utility grows business meaning, it belongs
in `src/domain` instead.
