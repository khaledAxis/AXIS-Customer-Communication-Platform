# Copy to a restricted external file, then set AXIS_WORKER_ENV_FILE to its path.
# Same dedicated secret as the app. No database or provider credentials belong here.
SCHEDULER_TRIGGER_SECRET=
# Compose supplies the private service URL. For a remote worker, use HTTPS.
SCHEDULER_URL=http://app:3000/api/internal/jobs/tick
